export class yatzyEngine {
  constructor() {
    this.scoreTable = {};
  }

  calculateScore(category, diceValues) {
    // Placeholder
    console.log(`Calculating score for ${category}...`);
    return 0;
  }

  isValidSelection(category, diceValues) {
    console.log(`Checking if ${category} is valid...`);
    return true;
  }
}
